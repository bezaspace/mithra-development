#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/back end"

if [ ! -d .venv ]; then
  echo "[sim] Setting up backend environment with uv sync..."
  uv sync
fi

exec uv run python scripts/run_simulation.py "$@"
